# ⚖️ Weight Converter (Pounds to Kilograms)

A simple JavaScript application that converts weight from pounds to kilograms in real time.

## 📌 Description
This project takes user input in pounds and automatically converts it to kilograms using JavaScript. It validates the input and displays errors for invalid values. The result updates dynamically as the user types.

## 🚀 Features
- Real-time conversion
- Input validation
- Error handling for invalid numbers
- Automatic result clearing after delay

## 🛠 Technologies Used
- HTML
- CSS
- JavaScript

## 🧠 What I Learned
- Input validation using JavaScript
- Handling user events with `input` listener
- Using `setTimeout` for delayed actions
- Updating DOM dynamically

## 🔢 Conversion Formula
Kilograms = Pounds ÷ 2.2

## 🔗 Live Demo
(Add GitHub Pages link here if deployed)
